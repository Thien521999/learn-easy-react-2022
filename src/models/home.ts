import { number } from "yup";

export interface product {
  category: String;
  createdAt: String;
  description: String;
  image: String;
  price: number;
  title: String;
  updatedAt: String;
  _id: String;
}
